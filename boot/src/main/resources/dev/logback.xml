<?xml version="1.0" encoding="UTF-8" ?>
<configuration>

    <appender name="FOX-FILE" class="ch.qos.logback.core.FileAppender">
        <file>/usr/local/fox/logs/web.log</file>
        <encoder>
            <pattern>%-4relative [%thread] %-5level %logger{35} - %msg %n</pattern>
        </encoder>
    </appender>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <!-- encoders are assigned the type
             ch.qos.logback.classic.encoder.PatternLayoutEncoder by default -->
        <encoder>
            <pattern>%-4relative [%thread] %-5level %logger{35} - %msg %n</pattern>
        </encoder>
    </appender>

    <appender name="FOX-WEB" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="FOX-FILE" />
    </appender>

    <root level="info">
        <appender-ref ref="FOX-WEB" />
        <appender-ref ref="CONSOLE" />
    </root>
</configuration>